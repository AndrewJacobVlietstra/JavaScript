const age = 19;
const isOldEnough = age >= 18;

// code inside if statement is only executed if the condition is true
// when false, the else code block will be executed instead, if its provided...
if (isOldEnough) {
    console.log("This person can start their driver's license!");
} else {
    console.log("This person is too young to start their driver's license!");
}

const birthYear = 1991;

if (birthYear <= 2000) {
    century = 20;
} else {
    century = 21;
}

console.log(century);